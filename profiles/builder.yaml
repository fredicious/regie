bindings:
  - { cli: codex, model: gpt-5.6-sol, auth: subscription }  # GPT-5.6 Sol — coding agent leader
  - { cli: claude, model: sonnet, auth: subscription }      # Sonnet 5 — cross-vendor fallback
budgets: { turns: 120, wall_minutes: 45, stall_minutes: 8 }
token_policy:
  effort: medium
  tools: [list, read, search, patch, shell]
  sandbox: workspace-write
  context_chars: 12000
