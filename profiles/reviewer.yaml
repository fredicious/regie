bindings:
  - { cli: claude, model: opus, auth: subscription }   # strongest critique; cross-vendor vs codex builder
  - { cli: claude, model: fable, auth: subscription }  # deadlocked reviews only
  - { cli: codex, model: gpt-5.6-sol, auth: subscription }
budgets: { turns: 60, wall_minutes: 30, stall_minutes: 8 }
hard: { cli: claude, model: fable, auth: subscription }
token_policy:
  effort: high
  tools: [list, read, search, shell]
  sandbox: read-only
  context_chars: 14000
  max_findings: 8
  max_finding_chars: 1200
